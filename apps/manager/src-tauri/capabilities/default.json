{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions used by the local Ardor Manager window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
