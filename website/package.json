{
  "name": "ardor-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "gen": "node scripts/gen-effects.mjs",
    "predev": "npm run gen",
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "npm run gen",
    "build": "astro build",
    "preview": "astro preview",
    "precheck": "npm run gen",
    "check": "astro check",
    "test:links": "node scripts/check-links.mjs",
    "test:copy": "node scripts/check-copy.mjs",
    "test": "npm run build && npm run test:links && npm run test:copy"
  },
  "dependencies": {
    "@fontsource/saira": "^5.3.0",
    "@fontsource/saira-condensed": "^5.3.0",
    "astro": "^7.2.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@fontsource/chakra-petch": "^5.1.0",
    "@fontsource/ibm-plex-mono": "^5.1.1",
    "@fontsource/open-sans": "^5.3.0",
    "@types/node": "^24.13.6",
    "typescript": "^5.6.3"
  }
}
